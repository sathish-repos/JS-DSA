# Recursion & Backtracking

**Real-world use case:** Pathfinding in games or robotics (e.g., solving a maze).

- Recursion is used for problems that can be broken into similar subproblems.
- Backtracking is used for constraint satisfaction (e.g., puzzles, pathfinding).

**Example:** See `recursion.js` for a maze-solving example.
