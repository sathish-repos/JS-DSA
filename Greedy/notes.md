# Greedy Algorithms

**Real-world use case:** Scheduling maximum number of non-overlapping meetings.

- Make the locally optimal choice at each step.
- Used in activity selection, Huffman coding, etc.

**Example:** See `greedy.js` for activity selection.
