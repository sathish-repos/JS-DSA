# Hash Tables

**Real-world use case:** Caching recently accessed web pages (e.g., browser cache).

- Store key-value pairs for fast lookup, insertion, and deletion.
- Used in databases, caches, and sets/maps in JavaScript.

**Example:** See `hashtables.js` for a simple cache implementation using Map.
